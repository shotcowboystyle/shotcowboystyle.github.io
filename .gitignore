# build output
dist/

# generated by the astro-sw integration from src/sw/ on every build; the
# cache-buster timestamp changed on each run, dirtying the tree constantly
public/sw.js

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

e2e/output
blob-report
lighthouse
stats.html
.stylelintcache
.cspellcache
.serena
.mcp.json

# per-developer lefthook overrides
lefthook-local.yml

# impeccable design tooling — user-local skill and generated caches
.claude/skills/
.claude/settings.local.json
.impeccable/config.local.json
.impeccable/hook.cache.json
.impeccable/live/

# lint caches
.eslintcache
