interface:
  display_name: 'Animaxxing'
  short_description: 'Reusable GSAP text and particle motion for any brand'
  default_prompt: "Use $animaxxing with the matching GSAP framework skill to add text and particle effects while preserving this site's design."

policy:
  allow_implicit_invocation: true
